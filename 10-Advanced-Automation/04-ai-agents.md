# AI Agents

AI agents can perform multi-step tasks such as reading files, editing code, running commands, or collecting information.

Use agents only when:

- the task is clearly bounded
- files are under version control
- raw data is protected
- the agent must report changes
- a human approves important steps

Agents are risky when the researcher cannot inspect what changed or verify the output.
